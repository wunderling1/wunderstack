/**
 * Re-export Lucide icons for app chrome. Apps import from `@wunderstack/ui` so they do not add
 * a direct `lucide-react` dependency (D3 / ask-before-adding).
 */
export {
  Activity,
  Bot,
  Building2,
  ChartLine,
  Menu,
  MessageCircle,
  Settings,
  X,
  type LucideIcon,
} from "lucide-react";
