# Code Review — 2026-07-10 14:00

## Scope
- **Time window:** Last 24 hours (since 2026-07-09 14:00) + all uncommitted/staged changes
- **Branch:** main (current)
- **Uncommitted included:** yes
- **Commits in scope:** 1
- **Changed files:** ~30 (1 committed + ~29 uncommitted)

---

## Changed files

### Commit `4b87992` — 2026-07-09 ~15:00 — ce1zer
> feat(eval): E7–E10 eval-gate hardening + v3 golden corpus

- `.env.example` (+2)
- `.github/workflows/ci.yml` (+22 / -1)
- `.gitignore` (+3)
- `packages/agents/scripts/build-golden-fixtures.ts` (deleted, -343)
- `packages/agents/src/cao/clarify.ts` (+8 / -2)
- `packages/agents/src/evals/baseline.ts` (+3)
- `packages/agents/src/evals/cao.eval.ts` (+many)
- `packages/agents/src/evals/fixtures/baseline.json` (replaced)
- `packages/agents/src/evals/fixtures/golden-passages.jsonl` (replaced: synthetic → real ETD)
- `packages/agents/src/evals/fixtures/golden-set.jsonl` (replaced: synthetic → real ETD)
- `packages/agents/src/evals/fixtures/golden-set.REVIEW.md` (new)
- `packages/agents/src/evals/golden-set.test.ts` (+17)
- `packages/agents/src/evals/golden-set.ts` (substantial)
- `packages/agents/src/evals/judge.ts` (+17)
- `packages/agents/src/evals/report-writer.ts` (new, +98)
- `packages/shared/src/env.ts` (+3)
- `scripts/ingest/chunk.ts` (+17)

### Uncommitted changes (working tree — effectively E11/E12/E13 work)

- `.env.example` (+4)
- `.github/workflows/ci.yml` (+21)
- `packages/agents/src/cao/agent.ts` (+25 / -7)
- `packages/agents/src/cao/prompt.ts` (+4)
- `packages/agents/src/cao/hard-facts.ts` (new, +67)
- `packages/agents/src/cao/hard-facts.test.ts` (new, +44)
- `packages/agents/src/evals/cao.eval.ts` (very large addition: Gate B-integration, Gate F)
- `packages/agents/src/evals/fixtures/baseline.json` (v3 → v4, answer section added)
- `packages/agents/src/evals/fixtures/golden-set.REVIEW.md` (v4 derived-cases review added)
- `packages/agents/src/evals/fixtures/golden-set.base.jsonl` (renamed from golden-set.jsonl, +3 derived cases)
- `packages/agents/src/evals/fixtures/golden-set.etd.jsonl` (new fund layer, 26 cases)
- `packages/agents/src/evals/golden-set.test.ts` (+66 lines for derived + fund-layer tests)
- `packages/agents/src/evals/golden-set.ts` (fund-layer loading, `derived` category)
- `packages/agents/src/evals/report-writer.ts` (v1 → v3 schema, added E11/E12 report shapes)
- `packages/ai/src/embeddings.ts` (minor)
- `packages/ai/src/http.ts` (new: `fetchWithRetry` with exponential backoff)
- `packages/ai/src/rerank.ts` (uses `fetchWithRetry`)
- `packages/db/src/client.ts` (`closeDb` added)
- `packages/db/src/index.ts` (exports `closeDb`)
- `packages/rag/src/index.ts` (re-exports `closeDb`, `RetrievedChunk`)
- `packages/shared/src/env.ts` (`EVAL_REQUIRE_DB` added)
- `scripts/eval/harvest-feedback.ts` (doc string update only)
- `scripts/ingest/fixtures.ts` (new, +191)
- `scripts/ingest/package.json` (`ingest:fixtures` script added)
- `scripts/ingest/run.ts` (`--force` flag added)

---

## Critical issues

| # | File:line | Category | Description | Suggestion |
|---|-----------|----------|-------------|-----------|
| 1 | `scripts/ingest/run.ts:61-64` | Correctness / Zod rule | `INGEST_CHUNK_CHARS` and `INGEST_OVERLAP_CHARS` are read directly from `process.env` without Zod validation. `Number(target)` silently produces `NaN` for non-numeric values, which then flows into the chunker as `targetChars: NaN`. This bypasses the `300-typescript.mdc` rule that all env vars must go through a Zod schema. | Add `INGEST_CHUNK_CHARS` and `INGEST_OVERLAP_CHARS` to the `envSchema` in `packages/shared/src/env.ts` with `z.coerce.number().int().positive()`. |
| 2 | `scripts/ingest/run.ts:128` | Data integrity | `sourceUri = basename(filePath)` (e.g. `cao.pdf`) is used as the unique key for the `onConflictDoUpdate`. If two different funds ever ingest files with the same basename (e.g. both ingesting `arbeidsrecht.pdf`), the second run silently overwrites the first record and changes its `fund`. The `eval-fixtures` script correctly uses a globally-unique scheme-prefixed URI (`eval-fixtures://golden-passages.jsonl`) showing the right pattern. Not triggered today (v1 is single-tenant), but the pattern is fragile. | Namespace the `sourceUri` per fund: `sourceUri = \`${options.fund}/${basename(filePath)}\`` — or use a scheme prefix like the fixture script does. |
| 3 | `packages/ai/src/rerank.ts:47` | Sovereignty / correctness | `RERANK_MODEL_REGISTRY` has only one entry: `"qwen3-embedding-8b"`. This is an **embedding** model name (the same string used in `EMBEDDING_CONFIG`). If Scaleway's `/v1/rerank` endpoint does not accept this model identifier, every rerank call fails silently at runtime with a non-OK response (caught by `fetchWithRetry` only if 429/5xx). The comment says "verified Jul 2026" but the name collision with the embedding model is a red flag worth a live sanity-check. Also, with only one entry in the registry and no non-sovereign option, adding a non-EU rerank model would require editing `RERANK_MODEL_REGISTRY` directly — the sovereignty guard is intentional but the naming needs to be verified. | Confirm with Scaleway's rerank API whether the correct model name for reranking is `"qwen3-embedding-8b"` or a distinct ID like `"bge-reranker-v2-m3"`. Update the registry accordingly and add a comment with the verified endpoint + model. |

---

## Warnings

| # | File:line | Category | Description | Suggestion |
|---|-----------|----------|-------------|-----------|
| 1 | `packages/agents/src/evals/cao.eval.ts:250` | Correctness | `PRODUCTION_DEFAULTS = caoQuestionSchema.parse({ question: "_", fund: EVAL_FIXTURE_FUND })` uses `"_"` as a placeholder question to extract default `topK`/`minScore`. This works but is fragile if the schema's validation of `question` is tightened in future (e.g. `min(3)`). | Read `topK`/`minScore` from the schema's `.default()` values or from `GENERATION_CONFIG`/`requireRerankConfig()` directly, without needing to parse a fake question. |
| 2 | `packages/agents/src/evals/cao.eval.ts:~240` | Consistency | `EVAL_FIXTURE_FUND = "eval-fixtures"` is hardcoded in both `cao.eval.ts` and `scripts/ingest/fixtures.ts`, with a comment "Kept in sync with...". Two duplicate literals with a manual sync requirement is exactly the silent drift risk the Zod-on-every-boundary rule aims to prevent. | Export `EVAL_FIXTURE_FUND` from a single location (e.g. `packages/shared/src/config/eval.ts`) and import it in both places. The rule of three isn't reached yet (two uses), but duplicating a key config string is low cost to fix and high cost to chase if it drifts. |
| 3 | `scripts/eval/harvest-feedback.ts:118` | TypeScript strictness | `const traceId = score.traceId as string` uses a type assertion on a `string | null | undefined` value. The `if (score.traceId && ...)` guard on line 102 filters out nullish values before `toCandidate` is called, but TypeScript doesn't narrow through array filter for `z.infer` types. Using `as string` violates the no-assertion rule in `300-typescript.mdc`. | Replace the type assertion with a non-null assertion or a typed guard: `const traceId: string = score.traceId ?? ""; if (!traceId) return ...`. Since the filter guarantees non-null at call site, `score.traceId!` (with a comment) or a refactored filter using a type predicate is more idiomatic. |
| 4 | `packages/agents/src/evals/fixtures/baseline.json:v4` | Quality signal | The newly recorded v4 answer baseline shows `underRefusalRate: 0.667` (2 of 3 refusal cases fail to refuse). This means the agent is answering when it should refuse on 2/3 of the ETD refusal probes against the **fixture** context (Gate C, in-memory). This is a notable quality regression compared to the old v1 corpus (0.3). The v4 baseline being recorded before these are fixed means CI will pass at this quality level, masking a real agent problem. | Investigate which 2 refusal cases the model answers instead of refusing (run Gate C locally, inspect individual case scores). Consider tightening the agent's system instructions for the ETD refusal cases, or flagging this as a tracked regression blocker before accepting the v4 baseline. |
| 5 | `packages/agents/src/evals/cao.eval.ts:~583` | Architecture | `retrievalIntegrationChecks()` and `fundLayerChecks()` run all retrieval queries in a sequential `for` loop (one `retrieveContext` call per query). For the current corpus size (~24 queries) this is fine, but if the fund layer grows to 100+ cases the nightly run time scales linearly. There is no concurrency limit or batching. | Track this as a nightly timing issue. When total queries exceed ~50, consider `Promise.all` with a semaphore-limited concurrency (e.g. `p-limit`). Do not optimize prematurely for the current 24-query corpus. |
| 6 | `packages/agents/src/evals/report-writer.ts:20` | Consistency | `EVAL_REPORT_SCHEMA_VERSION` jumped from `1` (in the committed version) to `3` (in the current working file) across two sessions without a changelog comment explaining what changed in each version. Anyone consuming `eval-report.json` artefacts must distinguish schema versions. | Add an inline changelog comment above `EVAL_REPORT_SCHEMA_VERSION`:  `// v1: initial (E9). v2: added retrievalIntegration. v3: added funds[]`. |
| 7 | `packages/agents/src/cao/agent.ts:~340` | UX | In the **streaming** path, when the E13 hard-fact guard fires (`hardFactGuardTriggered = true`), the client already received the streamed ungrounded prose. The `citations` event then tells the client to replace it with the not-found message. This means the user briefly sees an invented number before it's overwritten — a noticeable flash for slow connections. | Acceptable for now (the guard is for a low-frequency edge case). Track as a UX debt item: the guard fires after generation, so true prevention requires either a post-generation replacement UI contract (which already exists) or a structured-generation approach upstream. |
| 8 | `packages/agents/src/evals/cao.eval.ts` | Architecture / scope | Gate F (`fundLayerChecks`) is gated on `env.MISTRAL_API_KEY` because `fundCaseQuery` calls `condenseQuery` (LLM) for multi-turn cases. However, the ETD fund set currently has only 1 multi-turn case (etd-f23). The MISTRAL_API_KEY gate blocks ALL 26 cases even if condensation is only needed for 1. On a nightly run this isn't a problem (all keys present), but it makes local debugging harder. | Keep current behavior for simplicity. Document clearly in the CI workflow comment that MISTRAL_API_KEY is required for Gate F because of multi-turn condensation (not the majority of cases). |

---

## Suggestions

| # | File:line | Category | Description |
|---|-----------|----------|-------------|
| 1 | `packages/agents/src/evals/golden-set.ts:150` | Performance | `passageById(id)` performs a linear scan (`find`) through `goldenPassages` on every call. Each `passagesForCase()` call invokes it multiple times. For 31 passages this is imperceptible, but as the corpus grows it will become measurable. A `Map<string, GoldenPassage>` built once at module load would be O(1). |
| 2 | `scripts/ingest/fixtures.ts:182` | Dead code | `.then(() => { process.exitCode = 0; })` sets the exit code to its default value. The line is harmless but is dead code; remove it for clarity (the `.catch` handler already sets `process.exitCode = 1` on error). |
| 3 | `packages/agents/src/evals/cao.eval.ts` | Completeness | `retrievalIntegrationChecks()` excludes multi-turn cases (`testCase.history?.length === 0`). The integration gate therefore does not verify that the real pipeline correctly condenses and retrieves multi-turn questions. Gate B2 covers this in-memory but there's no integration equivalent. Consider a Gate B2-integration in a future phase. |
| 4 | `packages/agents/src/evals/golden-set.ts:267` | Robustness | `FUND_SET_FILE_RE = /^golden-set\.([a-z0-9-]+)\.jsonl$/` — the fund key regex allows hyphens, so a file like `golden-set.etd-v2.jsonl` would produce key `"etd-v2"`. Since FUND_SET_META is keyed by these tokens, any mismatch throws at module load. This fail-loud behavior is correct, but documenting the key naming convention (alphanumeric + hyphens allowed) in the REVIEW.md or a comment would prevent confusion. |
| 5 | `.github/workflows/ci.yml` | CI | The CI comment on the `EVAL_REQUIRE_ALL` env var ("Same-repo PRs have secrets...") references "audit point #13". The audit document (`eval-gate-audit.md`) should be linked explicitly or the CI YAML should reference a more stable doc location, since audit files may be renamed. |

---

## Dead code

| # | File:line | Type | Description |
|---|-----------|------|-------------|
| 1 | `scripts/ingest/fixtures.ts:182` | Unnecessary statement | `.then(() => { process.exitCode = 0; })` — exit code 0 is the default. The statement has no effect and contradicts the pattern used in `scripts/ingest/run.ts` which omits this. |

---

## Summary

- **Critical issues:** 3
- **Warnings:** 8
- **Suggestions:** 5
- **Dead-code items:** 1
- **Overall verdict:** Substantial, well-engineered batch of eval infrastructure (E7–E13). The architecture is sound — fixture hash guard, physical two-layer split, production-parity integration gate, E13 hard-fact runtime guard, and retry-with-backoff are all well-reasoned additions. Three issues need attention before this ships to a fund: unvalidated env vars in the ingest script (correctness/process-env bypass), a sourceUri namespace collision risk in multi-fund ingest, and a rerank model name that needs verification against the live Scaleway API. The quality regression in the v4 refusal baseline (67% under-refusal on Gate C) is the most operationally significant signal and warrants investigation before the baseline is accepted.

---

## Action plan

### 🔴 Now (today)

**1. `scripts/ingest/run.ts:61-64` — Add Zod validation for `INGEST_CHUNK_CHARS` / `INGEST_OVERLAP_CHARS`**
- **What:** Add `INGEST_CHUNK_CHARS: optional(z.coerce.number().int().positive())` and `INGEST_OVERLAP_CHARS: optional(z.coerce.number().int().positive())` to `envSchema` in `packages/shared/src/env.ts`. Remove the direct `process.env` access in `chunkOptionsFromEnv()` and read from `env` instead.
- **Why:** `Number(target)` produces `NaN` silently; `NaN` flowing into the chunker could corrupt chunk boundaries without any error. Violates the `300-typescript.mdc` Zod-on-every-boundary rule.
- **Estimate:** 15 min
- **Dependencies:** none

**2. `packages/ai/src/rerank.ts:47` — Verify rerank model name in RERANK_MODEL_REGISTRY**
- **What:** Make one live test call to Scaleway's `/v1/rerank` with `model: "qwen3-embedding-8b"` to confirm it is accepted. If rejected, find the correct model identifier and update `RERANK_MODEL_REGISTRY`. Update the comment with the verified API endpoint documentation URL.
- **Why:** The only registered rerank model shares its name with the embedding model. If the endpoint returns 4xx, all rerank calls fail silently at the sovereignty seam — the very place where an error is least visible.
- **Estimate:** 20 min (includes live API call)
- **Dependencies:** `SCALEWAY_API_KEY` in `.env`

**3. `packages/agents/src/evals/fixtures/baseline.json` — Investigate v4 under-refusal regression before accepting as the new bar**
- **What:** Run Gate C locally (`EVAL_WRITE_BASELINE=0`) against the ETD corpus and inspect which 2 of 3 refusal cases the model answers instead of refusing. Check whether the issue is in the agent prompt, the distractor passages chosen, or the judge's grading.
- **Why:** `underRefusalRate: 0.667` in the recorded baseline means CI will pass with 2/3 of refusal cases failing. Accepting this as the new baseline means no alert fires if the problem persists.
- **Estimate:** 30 min
- **Dependencies:** `SCALEWAY_API_KEY`, `MISTRAL_API_KEY`

### 🟡 This week

**4. `scripts/ingest/run.ts:128` — Namespace `sourceUri` per fund**
- **What:** Change `const sourceUri = basename(filePath)` to `const sourceUri = \`${options.fund}/${basename(filePath)}\``. Update the `onConflictDoUpdate` target accordingly (may need a migration if the existing DB row uses the old bare basename). The `eval-fixtures` fixture script already uses a globally-unique URI scheme and does not need to change.
- **Why:** Ingesting the same filename under two different funds silently overwrites records. Not exploited in v1 (single-tenant), but the O&O catalog pattern means multiple fund corpora will share CAO filenames.
- **Estimate:** 1 hour (including DB migration check)
- **Dependencies:** Confirm no production data uses the old bare-basename sourceUri (check DB before migrating).

**5. `packages/agents/src/evals/cao.eval.ts` and `scripts/ingest/fixtures.ts` — Single source for `EVAL_FIXTURE_FUND`**
- **What:** Export `EVAL_FIXTURE_FUND = "eval-fixtures"` from `packages/shared/src/config/eval.ts` (or a suitable existing config file). Import it in both `cao.eval.ts` and `scripts/ingest/fixtures.ts`.
- **Why:** Two duplicated literals with a "keep in sync" comment is a maintenance risk. The string is load-bearing: a mismatch means the integration gate and the ingest script target different funds and Gate B-integration will fail silently with empty results.
- **Estimate:** 20 min
- **Dependencies:** none

**6. `scripts/eval/harvest-feedback.ts:118` — Remove `as string` type assertion**
- **What:** Replace `const traceId = score.traceId as string` with a type-safe guard. The simplest fix: use a type predicate in the filter (`scores.filter((s): s is typeof s & { traceId: string } => Boolean(s.traceId) && ...)`) so TypeScript narrows the type, or accept the non-null assertion `score.traceId!` with a comment.
- **Why:** `as string` assertions are prohibited by `300-typescript.mdc` without an explanatory comment. This one is safe at runtime but sets a bad precedent.
- **Estimate:** 10 min
- **Dependencies:** none

**7. `packages/agents/src/evals/report-writer.ts:20` — Add schema version changelog comment**
- **What:** Add a brief inline changelog comment above `EVAL_REPORT_SCHEMA_VERSION = 3`: `// v1: initial (E9). v2: added retrievalIntegration (E11). v3: added funds[] (E12).`
- **Why:** Schema version numbers without a changelog are opaque to anyone (human or tooling) consuming the CI artifact.
- **Estimate:** 5 min
- **Dependencies:** none

### 🟢 When possible

**8. `packages/agents/src/evals/golden-set.ts:150` — Replace linear `passageById` scan with a Map**
- **What:** Build `const passageMap = new Map(goldenPassages.map(p => [p.id, p]))` at module load and rewrite `passageById` to return `passageMap.get(id)`.
- **Why:** O(N) scan on every lookup; currently 31 passages so invisible, but the corpus will grow.
- **Estimate:** 10 min
- **Dependencies:** none

**9. `scripts/ingest/fixtures.ts:182` — Remove dead `process.exitCode = 0`**
- **What:** Remove the `.then(() => { process.exitCode = 0; })` line.
- **Why:** Exit code 0 is the default and the line is dead code; its presence implies it has a purpose that it doesn't.
- **Estimate:** 2 min
- **Dependencies:** none

**10. Track Gate B2-integration gap**
- **What:** File a PLAN item for a "Gate B2-integration" that drives the real pipeline against multi-turn cases (condensation + retrieval) rather than the in-memory Gate B2. Not needed urgently — the current Gate B2 covers the fixture layer — but the integration gate skips multi-turn scenarios.
- **Why:** Documented gap: `retrievalIntegrationChecks()` explicitly excludes `history` cases. A nightly that passes Gate B2 but fails Gate B2-integration would go undetected.
- **Estimate:** design: 30 min; implementation: 2-4 hours
- **Dependencies:** Gate B-integration stable for ~2 weeks first (measure thresholds before adding more integration gates).

---

### Ordering rationale
Fix the Zod bypass first (correctness, could silently corrupt ingestion). Verify the rerank model name second (sovereignty seam — a misconfigured model there is a live compliance risk). Investigate the refusal baseline regression third (quality gate being accepted at a lower bar than it should be). The sourceUri namespacing and constant deduplication are this-week items since they address future-proofing for the multi-fund catalog, not an immediate production risk at single-tenant v1. Documentation and dead-code cleanup are last.
